mybatis:
    type-aliases-package: com.usst.cpf.springboot.entity

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    # mysql版本较高时需要指定useSSL=true
    url: jdbc:mysql://localhost:3306/mytest?useUnicode=true&characterEncoding=utf-8&useSSL=true
    username: root
    password: cpf0123456